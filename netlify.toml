[build]
  command = "npm run build"
  publish = "dist/client"

[build.environment]
  NODE_VERSION = "22"
  NPM_FLAGS = "--legacy-peer-deps"

# SPA fallback — all routes serve index.html so TanStack Router handles them
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
